# Telegram Bot Framework
aiogram>=3.10.0

# ML Framework (install separately with CUDA support)
# Compatible versions: PyTorch 2.5.x + pyannote.audio 3.3.2
# Install with: pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#
# NOTE: pyannote.audio 4.x requires torch==2.8.0 which doesn't exist yet
# Do NOT use PyTorch nightly (2.10+) as it breaks pyannote compatibility
#
# torch==2.5.1+cu121  # Installed separately via setup_environment.bat
# torchaudio==2.5.1+cu121  # Installed separately via setup_environment.bat

# Hugging Face Transformers
transformers>=4.35.0
accelerate>=0.24.0

# Speaker Diarization
pyannote.audio==4.0.0

# Audio Processing
ffmpeg-python>=0.2.0
pydub>=0.25.1
soundfile>=0.12.1
librosa>=0.10.0

# Configuration
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
PyYAML>=6.0.1
hydra-core>=1.3.0

# Async utilities
aiofiles>=23.2.1

# Logging
structlog>=23.2.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-mock>=3.12.0
